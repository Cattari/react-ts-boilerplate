import * as CONSTANTS from './core.constants';

export const checkAuth = () => async (dispatch) => {
  await d()
} action(CONSTANTS.CHECK_AUTH);
export const setAuthLoading = action(CONSTANTS.SET_AUTH_LOADER);